import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata = {
  title: "India's Premier Legal Document Service",
  description: "InLegalLaySum (Indian Legal Layman Summarization) dataset is a legal document summarization dataset focusing on simplified summaries of legal documents suitable for laypersons (who do not have formal legal training, i.e., layman in the perspective of Law)",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
